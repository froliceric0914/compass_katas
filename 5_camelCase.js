const camelCase = function(input) {};

//scan all the letter, once it is undefined, then delete it
//find the space, make the letter after space to chapital

console.log(caemCase("this is a string"));
console.log(caemCase("lighthouse labs"));
